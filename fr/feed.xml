---
layout: null
permalink: /fr/feed.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="fr">
  <title>Argent Public — Français</title>
  <subtitle>{{ site.tagline_fr | xml_escape }}</subtitle>
  <link href="{{ '/fr/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ '/fr/' | absolute_url }}" rel="alternate" type="text/html"/>
  <id>{{ '/fr/feed.xml' | absolute_url }}</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author><name>{{ site.author | xml_escape }}</name></author>
  <rights>{{ site.data.i18n.fr.rights | xml_escape }}</rights>

  {%- comment -%} Merge dossiers + explainers + record (FR), newest first {%- endcomment -%}
  {%- assign fr_dossiers = site.dossiers | where: "lang", "fr" -%}
  {%- assign fr_explainers = site.explainers | where: "lang", "fr" -%}
  {%- assign fr_record = site.record | where: "lang", "fr" -%}
  {%- assign merged = fr_dossiers | concat: fr_explainers | concat: fr_record | sort: "date" | reverse -%}
  {%- for item in merged limit: 50 -%}
  <entry>
    <title>{{ item.title | xml_escape }}</title>
    <link href="{{ item.url | absolute_url }}" rel="alternate" type="text/html"/>
    <id>{{ item.url | absolute_url }}</id>
    {%- assign d = item.date | default: item.doc_date -%}
    {%- if d -%}<published>{{ d | date_to_xmlschema }}</published>
    <updated>{{ item.updated | default: d | date_to_xmlschema }}</updated>{%- endif -%}
    {%- if item.summary -%}<summary>{{ item.summary | xml_escape }}</summary>
    {%- elsif item.description -%}<summary>{{ item.description | strip_html | xml_escape }}</summary>{%- endif -%}
  </entry>
  {%- endfor -%}
</feed>
